# Тестирование раскрашивания через веб-интерфейс

## Проблема
Раскрашивание ячеек не работает при проверке через браузер.

## Исправление
Добавлен `confidence_score` в словарь результатов в `batch_processor/workers/processing_task.py`.

## Проверка исправления

### 1. Локальный тест (✅ Работает)

Локальный тест подтверждает, что функция раскрашивания работает корректно:

```bash
python test_real_color_coding.py
python tools/debug/verify_colors.py
```

Результат:
- Row 2 (score 1.0): Без цвета ✓
- Row 3 (score 0.850): Зеленый (0000FF00) ✓
- Row 4 (score 0.120): Красный (00FF0000) ✓

### 2. Проверка через веб-интерфейс

#### Шаг 1: Запустите веб-приложение

```bash
python start_batch_web.py
```

#### Шаг 2: Откройте браузер

Перейдите по адресу: http://localhost:8000

Войдите с учетными данными:
- Username: `admin`
- Password: `admin123`

#### Шаг 3: Загрузите тестовый файл

Используйте файл `test_web_color_coding.xlsx` (создан автоматически).

#### Шаг 4: Настройте параметры обработки

- **Processing Mode**: All rows
- **Algorithm**: similarity_top1
- **URL Processing**: Enabled (если есть URL колонка)

#### Шаг 5: Запустите обработку

Нажмите "Start Processing" и дождитесь завершения.

#### Шаг 6: Скачайте результат

После завершения обработки скачайте файл результатов.

#### Шаг 7: Проверьте раскрашивание

Откройте скачанный файл в Excel и проверьте колонку `TNVED_Code`:

**Ожидаемые цвета:**

| Confidence Score | Цвет | Описание |
|-----------------|------|----------|
| = 1.0 | ⚪ Белый (без цвета) | Точное совпадение URL |
| >= 0.185 | 🟢 Зеленый | Высокая уверенность |
| < 0.185 | 🔴 Красный | Низкая уверенность |

### 3. Отладка проблем

#### Проблема: Цвета не применяются

**Возможные причины:**

1. **Веб-приложение не перезапущено после изменений**
   - Решение: Остановите и перезапустите `start_batch_web.py`

2. **Celery worker не перезапущен**
   - Решение: Перезапустите Celery worker:
     ```bash
     celery -A batch_processor.workers.celery_app worker --loglevel=info --pool=solo
     ```

3. **Кэш Redis содержит старые данные**
   - Решение: Очистите Redis:
     ```bash
     redis-cli FLUSHALL
     ```

4. **Файл открыт в Excel во время обработки**
   - Решение: Закройте все Excel файлы перед обработкой

#### Проверка логов

Проверьте логи на наличие сообщения:
```
Applied color coding to X cells
```

Если это сообщение есть, значит раскрашивание выполнено.

#### Проверка с помощью скрипта

Используйте скрипт для проверки цветов в результирующем файле:

```bash
python tools/debug/verify_colors.py
```

Замените имя файла в скрипте на имя вашего результирующего файла.

### 4. Технические детали

#### Где применяется раскрашивание

1. **ExcelProcessor.write_results()** - записывает данные в Excel
2. **ExcelProcessor._apply_color_coding()** - применяет цвета к ячейкам

#### Логика раскрашивания

```python
if confidence_score == 1.0:
    # URL match - no color (leave default)
    pass
elif confidence_score >= 0.185:
    # High confidence - green
    cell.fill = green_fill
else:
    # Low confidence - red
    cell.fill = red_fill
```

#### Цветовые коды

- Зеленый: `PatternFill(start_color="00FF00", end_color="00FF00", fill_type="solid")`
- Красный: `PatternFill(start_color="FF0000", end_color="FF0000", fill_type="solid")`

### 5. Проверочный список

- [ ] Код изменен в `batch_processor/workers/processing_task.py`
- [ ] Веб-приложение перезапущено
- [ ] Celery worker перезапущен (если используется)
- [ ] Redis очищен (если используется)
- [ ] Тестовый файл загружен через веб-интерфейс
- [ ] Обработка завершена успешно
- [ ] Результирующий файл скачан
- [ ] Цвета проверены в Excel
- [ ] Логи содержат сообщение "Applied color coding to X cells"

## Заключение

Исправление протестировано локально и работает корректно. Если раскрашивание не работает через веб-интерфейс, убедитесь, что:

1. Все компоненты перезапущены после изменений
2. Используется правильная версия кода
3. Нет кэшированных данных в Redis
4. Логи показывают, что раскрашивание применяется

Если проблема сохраняется, проверьте логи на наличие ошибок и убедитесь, что `confidence_score` передается в `results_dict`.
